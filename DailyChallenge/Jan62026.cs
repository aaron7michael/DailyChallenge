/// <summary>
/// From: https://www.geeksforgeeks.org/problems/max-xor-subarray-of-size-k/1
/// Given an array of integers arr[]  and a number k. Return the maximum xor of a subarray of size k.
/// Input: arr[] = [2, 5, 8, 1, 1, 3], k = 3
/// Output: 15
/// Explanation: arr[0] ^ arr[1] ^ arr[2] = 15, which is maximum.
/// 
/// Note: A subarray is a contiguous part of any given array.
/// </summary>
using System;
public class MaxXORFromArray
{
    public static int GetMaxXORFromArray(int[] arr, int k, int i, int currMax) 
    {
        if (k >= arr.Length)
        {

        }
        return 0;
    }
}

